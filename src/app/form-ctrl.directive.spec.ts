import { FormCtrlDirective } from './form-ctrl.directive';

describe('FormCtrlDirective', () => {
  it('should create an instance', () => {
    const directive = new FormCtrlDirective();
    expect(directive).toBeTruthy();
  });
});
